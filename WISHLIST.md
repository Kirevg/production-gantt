# Wish List - Список желаемых функций

## Автобэкап базы данных

### Описание
Настройка автоматического резервного копирования базы данных на сервере БД с отправкой файла бэкапа по электронной почте.

### Варианты реализации

#### Вариант 1: PostgreSQL pg_dump (если база на вашем сервере)
- Скрипт на сервере БД делает `pg_dump`
- Сохраняет в файл
- Отправляет по email через `mail`, `sendmail` или `msmtp`
- Cron запускает по расписанию (например, ежедневно в 2:00)

#### Вариант 2: Neon.tech (облачная БД) — через API бэкапа
- Скрипт на вашем сервере/VM вызывает ваш API (`/api/backup/create`)
- Сохраняет JSON-файл
- Отправляет по email
- Cron запускает по расписанию

### Что нужно для реализации:
1. **Скрипт на bash/Python/Node.js**
   - Подключается к БД или вызывает API
   - Создает бэкап
   - Отправляет по email (через SMTP)

2. **Настройка почты на сервере**
   - `msmtp`, `sendmail` или SMTP-клиент в скрипте

3. **Cron job**
   - Расписание (например, `0 2 * * *` — каждый день в 2:00)

4. **Ротация бэкапов**
   - Очистка старых бэкапов, если хранить на диске

### Преимущества:
- ✅ Не зависит от контейнера приложения
- ✅ Бэкап приходит на почту автоматически
- ✅ Можно настроить несколько получателей
- ✅ Можно добавить логирование и мониторинг

### Учтите:
- ⚠️ **Размер вложений**: если база большая, почта может не пропустить
- ⚠️ **Безопасность**: вложенные файлы лучше шифровать
- ⚠️ **Альтернатива для больших бэкапов**: отправлять ссылку на облачное хранилище

### Статус
🔲 Не реализовано

---
