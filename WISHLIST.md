# Wish List - Список желаемых функций

## Автобэкап базы данных

### Описание
Настройка автоматического резервного копирования базы данных на сервере БД с отправкой файла бэкапа по электронной почте.

### Варианты реализации

#### Вариант 1: PostgreSQL pg_dump (если база на вашем сервере)
- Скрипт на сервере БД делает `pg_dump`
- Сохраняет в файл
- Отправляет по email через `mail`, `sendmail` или `msmtp`
- Cron запускает по расписанию (например, ежедневно в 2:00)

#### Вариант 2: Neon.tech (облачная БД) — через API бэкапа
- Скрипт на вашем сервере/VM вызывает ваш API (`/api/backup/create`)
- Сохраняет JSON-файл
- Отправляет по email
- Cron запускает по расписанию

### Что нужно для реализации:
1. **Скрипт на bash/Python/Node.js**
   - Подключается к БД или вызывает API
   - Создает бэкап
   - Отправляет по email (через SMTP)

2. **Настройка почты на сервере**
   - `msmtp`, `sendmail` или SMTP-клиент в скрипте

3. **Cron job**
   - Расписание (например, `0 2 * * *` — каждый день в 2:00)

4. **Ротация бэкапов**
   - Очистка старых бэкапов, если хранить на диске

### Преимущества:
- ✅ Не зависит от контейнера приложения
- ✅ Бэкап приходит на почту автоматически
- ✅ Можно настроить несколько получателей
- ✅ Можно добавить логирование и мониторинг

### Учтите:
- ⚠️ **Размер вложений**: если база большая, почта может не пропустить
- ⚠️ **Безопасность**: вложенные файлы лучше шифровать
- ⚠️ **Альтернатива для больших бэкапов**: отправлять ссылку на облачное хранилище

### Статус
🔲 Не реализовано

---

## Логирование действий пользователей

### Описание
Настройка системы логирования действий пользователей: кто, когда, что делал (вход, выход, изменения, добавления и т.д.).

### Что нужно логировать:
1. **Аутентификация**
   - Вход в систему (login)
   - Выход из системы (logout)
   - Неудачные попытки входа
   - Смена пароля

2. **Операции с данными**
   - Создание записей (проекты, изделия, этапы работ, пользователи и т.д.)
   - Редактирование записей (изменения полей)
   - Удаление записей
   - Восстановление записей (если есть)

3. **Системные операции**
   - Создание/восстановление бэкапов
   - Применение миграций
   - Изменение настроек системы
   - Изменение прав доступа пользователей

4. **Просмотр данных**
   - Просмотр чувствительных данных (опционально)
   - Экспорт данных

### Варианты реализации:

#### Вариант 1: Использование существующей таблицы AuditLog
- Расширить модель `AuditLog` в Prisma
- Создать middleware для автоматического логирования
- Добавить логирование на уровне API endpoints

#### Вариант 2: Создать отдельную систему логирования
- Новая таблица `UserActivityLog` или расширение `AuditLog`
- Middleware для автоматического перехвата действий
- API endpoints для просмотра логов

### Что нужно для реализации:
1. **Расширение модели данных**
   - Поля: userId, action, entityType, entityId, details, timestamp, ipAddress
   - Индексы для быстрого поиска по пользователю и дате

2. **Middleware для автоматического логирования**
   - Перехват всех запросов к API
   - Определение типа действия (CREATE, UPDATE, DELETE, VIEW)
   - Сохранение в базу данных

3. **Интерфейс для просмотра логов**
   - Страница в админ-панели
   - Фильтры по пользователю, дате, типу действия
   - Экспорт логов

4. **Очистка старых логов**
   - Ротация логов (удаление старых записей)
   - Архивирование логов

### Преимущества:
- ✅ Аудит безопасности
- ✅ Отслеживание изменений
- ✅ Восстановление истории операций
- ✅ Соответствие требованиям безопасности

### Учтите:
- ⚠️ **Производительность**: большое количество логов может замедлить работу
- ⚠️ **Хранение**: нужно предусмотреть ротацию и архивацию
- ⚠️ **Конфиденциальность**: логи могут содержать чувствительные данные
- ⚠️ **Размер БД**: логи могут значительно увеличить размер базы данных

### Статус
🔲 Не реализовано

---
