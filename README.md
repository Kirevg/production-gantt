# Production Gantt - Система управления проектами

## 🚀 Быстрый старт

### Предварительные требования
- Node.js 18+
- npm или yarn
- Аккаунт на Neon.tech (для базы данных PostgreSQL)

### Установка на новом компьютере

#### 1. Клонирование репозитория
```bash
git clone <your-repo-url>
cd production-gantt
```

#### 2. Установка зависимостей

**Backend (API):**
```bash
cd api
npm install
```

**Frontend:**
```bash
cd ../web
npm install
```

#### 3. Настройка переменных окружения

**Backend - создайте файл `api/.env` на основе шаблона:**
```bash
cd api
cp env.example .env
```

Затем отредактируйте `api/.env` и укажите:
- **DATABASE_URL** - connection string от Neon.tech (получите на https://neon.tech)
- **JWT_SECRET** - секретный ключ для JWT токенов (придумайте уникальный)
- **PORT** - порт API (по умолчанию 4000)
- **CORS_ORIGINS** - разрешенные origins для CORS

Пример:
```env
DATABASE_URL="postgresql://username:password@hostname:port/database?sslmode=require"
JWT_SECRET="your-super-secret-jwt-key-here-change-this-in-production"
PORT=4000
CORS_ORIGINS="http://localhost,http://localhost:5173,http://localhost:80"
```

**Frontend - создайте файл `web/.env` на основе шаблона:**
```bash
cd ../web
cp .env.example .env
```

Отредактируйте `web/.env` (обычно не требует изменений для локальной разработки):
```env
VITE_API_BASE_URL=http://localhost:4000
```

#### 4. Настройка базы данных с Prisma

**Генерация Prisma Client:**
```bash
cd api
npx prisma generate
```

**Применение миграций:**
```bash
npx prisma migrate deploy
```

**Или для разработки (создает базу заново):**
```bash
npx prisma migrate dev
```

⚠️ **Внимание:** `migrate dev` может пересоздать базу данных. Используйте `migrate deploy` для существующей базы.

**Просмотр базы данных (Prisma Studio):**
```bash
npx prisma studio
```

#### 5. Запуск проекта

**Запуск серверов (PowerShell):**
```powershell
.\restart-servers.ps1
```

**Или вручную:**

В одном терминале (Backend):
```bash
cd api
npm run dev
```

В другом терминале (Frontend):
```bash
cd web
npm run dev
```

#### 6. Доступ к приложению
- Frontend: http://localhost:5173
- API: http://localhost:4000
- Prisma Studio: http://localhost:5555 (если запущен)

## 📁 Структура проекта

```
production-gantt/
├── api/                 # Backend (Node.js + Express + Prisma)
│   ├── src/
│   ├── prisma/
│   │   └── schema.prisma  # Схема базы данных
│   ├── .env              # ⚠️ НЕ коммитится в Git
│   └── env.example       # ✅ Шаблон для .env
├── web/                 # Frontend (React + TypeScript + Vite)
│   ├── src/
│   ├── .env              # ⚠️ НЕ коммитится в Git
│   └── .env.example      # ✅ Шаблон для .env
└── README.md
```

## 🔐 Важные файлы

### Переменные окружения
- **`api/.env`** - настройки Backend (база данных, JWT секрет)
- **`web/.env`** - настройки Frontend (URL API)
- **`.env.example`** - шаблоны, которые коммитятся в Git

⚠️ **ВАЖНО:** Файлы `.env` содержат секретные данные и НЕ должны коммититься в Git!

### База данных
- Используется **Neon.tech** (облачная PostgreSQL)
- Connection string получается на https://neon.tech
- Prisma используется для работы с БД

## 🛠️ Полезные команды

### Prisma
```bash
# Генерация Prisma Client
npx prisma generate

# Применение миграций
npx prisma migrate deploy

# Просмотр базы данных
npx prisma studio

# Сброс базы данных (⚠️ удалит все данные!)
npx prisma migrate reset
```

### Backend
```bash
cd api
npm run dev        # Запуск в режиме разработки
npm run build      # Сборка проекта
npm start          # Запуск production версии
```

### Frontend
```bash
cd web
npm run dev        # Запуск dev сервера
npm run build      # Сборка для production
npm run preview    # Просмотр production сборки
```

## 📝 Примечания

- При работе на нескольких компьютерах нужно:
  1. Скопировать `env.example` в `.env`
  2. Заполнить свои значения (особенно DATABASE_URL и JWT_SECRET)
  3. Выполнить `npx prisma generate` в папке `api`
  4. Применить миграции `npx prisma migrate deploy`

- База данных одна на всех (Neon.tech), поэтому миграции применяются один раз
- JWT_SECRET должен быть одинаковым на всех компьютерах, если нужна совместимость токенов
